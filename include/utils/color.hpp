#ifndef VIRTUAL_GALAXY_COLOR_HPP
#define VIRTUAL_GALAXY_COLOR_HPP

#include "base.hpp"

namespace color {
    uint32 rgbaToInteger(uint8 r, uint8 g, uint8 b, uint8 a);
    uint32 rgbToInteger(uint8 r, uint8 g, uint8 b);
}

#endif //VIRTUAL_GALAXY_COLOR_HPP
